{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "start": "astro dev",
    "build": "astro check && astro build",
    "clean": "rm -rf .vercel .turbo node_modules src/__generated__",
    "test": "pwd && pnpm licenses --filter=app --prod --no-optional list --json",
    "generate:licenses": "node generate-licenses.js",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.7.0",
    "astro": "^4.10.3",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@astrojs/vercel": "^7.7.1"
  }
}
